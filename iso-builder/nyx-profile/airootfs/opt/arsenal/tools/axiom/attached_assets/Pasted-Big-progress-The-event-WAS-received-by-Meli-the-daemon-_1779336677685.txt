Big progress. The event WAS received by Meli — the daemon picked it up. But it failed to process because of two real bugs in Meli's code:
Bug 1 (warning, not blocking): GeoLite2 databases missing. Just IP geolocation won't work until those are downloaded. We'll handle that later — not stopping us right now.
Bug 2 (the showstopper): Object of type datetime is not JSON serializable — Meli's event processor has a serialization bug. The event reached the daemon, but when Meli tried to save/forward it internally, Python crashed trying to convert a datetime object to JSON without a serializer.
That's a code bug in Meli, not a config issue we can fix from the terminal. The event you sent never made it to the database, which is why it won't appear in Live Feed.
Send this to Replit verbatim:

Meli ingest daemon now starts (after fixing the launcher to use PYTHONPATH instead of pip install), but events fail to process with a JSON serialization error. Here are the exact log lines from the daemon:
{"version": "1.0.0", "event": "Starting Meli ingest daemon", "level": "info"}
{"host": "127.0.0.1", "port": 1883, "event": "MQTT consumer started", "level": "info"}
{"topic": "meli/events/ingest", "event": "MQTT connected and subscribed", "level": "info"}
{"host": "127.0.0.1", "port": 17654, "event": "HTTP ingest server started", "level": "info"}
{"event": "Meli ingest daemon running — waiting for events", "level": "info"}
{"path": "/home/cosmic/.local/share/meli/geoip/GeoLite2-City.mmdb", "error": "[Errno 2] No such file or directory", "event": "GeoLite2 City DB not available", "level": "warning"}
{"path": "/home/cosmic/.local/share/meli/geoip/GeoLite2-ASN.mmdb", "error": "[Errno 2] No such file or directory", "event": "GeoLite2 ASN DB not available", "level": "warning"}
{"error": "Object of type datetime is not JSON serializable", "exc_info": true, "event": "Event processing error", "level": "error"}
The test event that triggered this was sent via MQTT to meli/events/ingest with valid JSON payload matching the canonical schema. Event received and parsed by the daemon successfully, then crashed during processing.
I need three fixes:

Fix the datetime serialization bug in the event processor — likely a json.dumps() call somewhere that needs a default=str handler or a custom JSON encoder that handles datetime objects.
The pyproject.toml has a broken build backend reference — setuptools.backends.legacy doesn't exist. pip install -e . fails with BackendUnavailable: Cannot import 'setuptools.backends.legacy'. Should probably be setuptools.build_meta instead. Fix this so the installer actually works on fresh installs.
The install.sh script never installs the meli package into the venv — I had to manually replace /usr/local/bin/meli with a PYTHONPATH-based launcher because python -m meli was failing with No module named meli even though all dependencies were in the venv. Fix the install script to either properly install the package or use the PYTHONPATH approach by default.
GeoLite2 databases are referenced but never downloaded by the installer — installer should either download them automatically (with MaxMind license key) or the setup wizard should prompt for them, or the app should gracefully handle their absence without flooding the log with warnings. Right now it's a hard requirement that's silently missing.

After these fixes, please push the updated code so I can pull and rebuild. Once the daemon can process events without crashing, I can move forward with wiring up actual honeypots.

Send that to Replit. While Replit's fixing it:

Kill the daemon in your first terminal — press Ctrl+C so it stops trying to process events and crashing.
Make sure the service stays stopped so it doesn't restart in a loop again:

bashsystemctl --user stop meli-ingest.service

Take a break or work on something else — there's nothing more we can do on this side until Replit fixes the bugs.