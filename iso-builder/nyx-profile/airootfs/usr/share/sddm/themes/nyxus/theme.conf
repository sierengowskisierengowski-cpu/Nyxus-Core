[General]
# rev 2026-05-14 r17 — Eclipse reference is the LOCKED visual target for
# every NYXUS chrome surface (see docs/brand/VISUAL-TARGET.md). The login
# greeter is the very first frame the user sees, so it must lead with the
# canonical image: black Eclipse mark with cream halo over a still
# reflective surface. Earlier `background.png` was a generic void.
background=backgrounds/nyxus-eclipse-reference.png
type=image
blur=false
recursiveBlur=false
blurRadius=0

PasswordFieldCharacter=•
PasswordFieldPlaceholderText=enter passphrase

LoginButtonText=SIGN IN

ScreenPadding=0
FontPointSize=11

# DARK GLASS LOGIN — palette/typography lives in Main.qml.
# Locked Visual System rev 2026-05-06v.
