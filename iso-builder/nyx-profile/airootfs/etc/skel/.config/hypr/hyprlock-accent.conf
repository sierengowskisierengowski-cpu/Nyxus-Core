# NYXUS hyprlock accent — auto-managed at runtime by `nyxus-apply-accent`.
# Shipped empty so hyprlock.conf's `source = ~/.config/hypr/hyprlock-accent.conf`
# resolves on a fresh account before the accent engine first runs.
