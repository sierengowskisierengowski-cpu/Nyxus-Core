# NYXUS · system-wide environment           rev 2026-05-14 r1 (Sprint F)
# Loaded by PAM (pam_env) early in every login session — covers Wayland,
# X11, login shell, and SDDM. Keep this minimal: PAM doesn't run a shell,
# so no expansion is performed (literal values only).

# ── Qt platform integration (Sprint F) ────────────────────────────────
# Without these, every Qt app (Krita, Kdenlive, qBittorrent, OBS, VLC,
# digiKam, Telegram, etc.) renders with stock Breeze and looks alien
# inside the NYXUS shell. With them, qt5ct/qt6ct route through Kvantum,
# which renders the NYXUS Kvantum theme matching GTK NYXUS-Dark.
# Note on Qt5 vs Qt6 routing: pam_env sets ONE value per var, so we can't
# set qt5ct for Qt5 apps AND qt6ct for Qt6 apps. We pick qt5ct (Qt5 has
# the larger user-facing app surface — Krita, qBittorrent, Telegram,
# OBS, VLC, digiKam). Qt6 apps fall back to QT_STYLE_OVERRIDE=kvantum
# below, which still applies the NYXUS Kvantum widget style — they
# just don't pick up qt6ct's icon-theme/font routing. Power users can
# override per-session by setting QT_QPA_PLATFORMTHEME=qt6ct in their
# shell rc; both qt5ct and qt6ct configs are pre-shipped under
# /etc/skel/.config/.
QT_QPA_PLATFORMTHEME=qt5ct
QT_STYLE_OVERRIDE=kvantum
QT_AUTO_SCREEN_SCALE_FACTOR=1
QT_WAYLAND_DISABLE_WINDOWDECORATION=1

# ── GTK polish ────────────────────────────────────────────────────────
GTK_THEME=NYXUS-Dark
GTK_USE_PORTAL=1

# ── Cursor ────────────────────────────────────────────────────────────
XCURSOR_THEME=NYXUS-Aurora
XCURSOR_SIZE=24

# ── Wayland defaults ──────────────────────────────────────────────────
MOZ_ENABLE_WAYLAND=1
ELECTRON_OZONE_PLATFORM_HINT=auto
_JAVA_AWT_WM_NONREPARENTING=1

# ── XDG ───────────────────────────────────────────────────────────────
XDG_CURRENT_DESKTOP=Hyprland
XDG_SESSION_TYPE=wayland
