# NYXUS · zram-generator config · rev 2026-05-13 r1
# Compressed RAM-backed swap. Effectively doubles usable RAM on small
# machines. zstd compresses ~3x with negligible CPU cost.
[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
