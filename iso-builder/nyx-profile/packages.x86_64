# ============================================
# NYXUS
# Copyright © 2026 Joseph A. Sierengowski
# All Rights Reserved
# NYX-J5W-2026-SIERENGOWSKI-LOCKED
# ============================================
#
# Pacman packages baked into the NYX live ISO.
# Comments + blank lines are ignored by mkarchiso.

# ── base system ────────────────────────────────────────────────────
base
base-devel
linux
linux-firmware
mkinitcpio
mkinitcpio-archiso
intel-ucode
amd-ucode
sudo
networkmanager
iwd
wpa_supplicant
dhcpcd
openssh
git
nano
vim
wget
curl
unzip
zip
tar
htop
# btop was NEVER in this list, yet the profile ships /etc/skel/.config/btop/
# (btop.conf + nyxus-prism.theme + SharkDash.theme + nyxus-hacker.theme) and
# four shipped launch paths fall back to it (stations GHOST + MESH, the OPS
# deck quick-launch grid, nyxus-hacker-mode's btop theme swap). Verified
# absent from the 2026.07.29 image: no /usr/bin/btop anywhere in the squashfs.
# So the whole Jul-29 hacker-mode btop work was dead on the stick and two
# station fallbacks resolved to nothing. (2026-07-30)
btop
fastfetch

# ── boot ───────────────────────────────────────────────────────────
syslinux
grub
efibootmgr
edk2-shell
memtest86+
memtest86+-efi

# ── filesystem support ─────────────────────────────────────────────
btrfs-progs
dosfstools
e2fsprogs
exfatprogs
f2fs-tools
ntfs-3g
xfsprogs
gptfdisk
parted
mtools

# ── Hyprland / Wayland ────────────────────────────────────────────
hyprland
hyprlock
hypridle
# mpvpaper is AUR-only — built from source by customize_airootfs.sh.
# These are its runtime + build dependencies.
mpv
meson
ninja
scdoc
# rust provides `cargo` — required to build EWW from source in
# customize_airootfs.sh. The chroot has no pacman mirrors, so build deps
# CANNOT be installed at customize time; they MUST be pacstrapped here.
rust
wayland-protocols
hyprcursor
greetd
plymouth
qt5-quickcontrols2
qt5-graphicaleffects
qt5-declarative
# waybar removed 2026-05-11 — replaced by EWW (ElKowar's Wacky Widgets).
# EWW is AUR-only (eww-wayland); built from source by customize_airootfs.sh.
# Runtime helpers EWW expects:
socat
jq
acpi
gtk-layer-shell
wofi
rofi
rofi-emoji
dunst
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
qt5-wayland
qt6-wayland
xorg-xwayland
wl-clipboard
grim
slurp
cliphist
brightnessctl
playerctl
pamixer
pavucontrol
swaybg
# rev 2026-07-16: `swww` no longer resolves in Arch `extra` — upstream
# repos dropped it in favor of `awww` ("An Answer to your Wayland
# Wallpaper Woes"), which is a maintained fork/successor, not a
# different project as an earlier comment here claimed — its own
# package metadata declares `Provides: swww` + `Replaces: swww`. Package
# name swapped back to `awww`; a `swww`→`awww` compat symlink is added
# by customize_airootfs.sh so every existing `swww ...`-invoking script
# (nyxus-set-wallpaper.sh, nyxus-wallpaper-autostart, nyxus_wallpaper_
# studio.py, etc.) keeps working unmodified.
awww
wdisplays
# wlogout removed 2026-05-11 — built from upstream source by
# customize_airootfs.sh (was failing pacstrap on mirrors that don't yet
# carry it in extra; building from source removes the mirror dependency).
nwg-displays
blueman
geoclue
power-profiles-daemon
bluez
bluez-utils
# (dedup) networkmanager
network-manager-applet
polkit
polkit-gnome

# ── audio ─────────────────────────────────────────────────────────
pipewire
pipewire-alsa
pipewire-pulse
pipewire-jack
wireplumber
alsa-utils
# cava — console audio visualizer. REQUIRED, not optional eye candy: it
# drives the eww CAVA bar widget, the NYXUS PULSE audio-reactive window
# halo (hypr/scripts/nyxus-pulse.sh via ~/.config/nyxus/pulse-cava.conf),
# and the signature-bar spectrum. Was referenced everywhere but never
# listed here, so the music-reactive features were dead on a real install.
cava

# ── GTK4 + Python (for NYXUS apps) ────────────────────────────────
gtk4
libadwaita
adw-gtk-theme
papirus-icon-theme
vte3
vte-common
python
python-gobject
python-pip
python-cairo
python-psutil
python-cryptography
python-requests
python-pillow
python-dnspython

# ── nyxus-intel runtime deps (Policy A — pacman-first, offline-capable) ─
# Mirrors the contents of intel/packages.txt + intel/requirements.txt
# packaged for Arch. Anything not in official repos stays as a pip-at-
# first-run fallback inside install.sh.
python-bcrypt
python-reportlab
# NOTE: `python-shodan` is AUR-only — install.sh handles it as a pip
# fallback at first run if needed (nyxus-phantom threat-intel feature).
# NOTE: `python-piexif` is AUR-only — install.sh handles it as a pip
# fallback at first run (EXIF metadata editing for the media library).
# NOTE: `python-whois` is AUR-only — install.sh handles it as a pip
# fallback at first run. The `whois` C binary below covers the CLI need.
whois

# ── fonts ─────────────────────────────────────────────────────────
ttf-jetbrains-mono-nerd
ttf-firacode-nerd
inter-font
noto-fonts
noto-fonts-emoji
otf-font-awesome

# ── terminal emulators (alacritty is NYXUS default) ───────────────
alacritty

# ── terminal + utilities ──────────────────────────────────────────
foot
kitty
fzf
ripgrep
fd
bat
eza
zoxide
starship
ranger


# ── GPU drivers — NVIDIA hybrid stack (LOCKED · 2026-05-10 r5) ────
# Target hw: MSI GS77 i9-12900H + RTX 3060 (Intel iGPU + NVIDIA dGPU).
# nvidia-open-dkms rebuilds the module against any kernel; linux-headers is
# required for DKMS to compile. egl-wayland + libva-nvidia-driver are
# required for proper Wayland + hardware video decode under Hyprland.
linux-headers
nvidia-open-dkms
nvidia-utils
nvidia-settings
lib32-nvidia-utils
opencl-nvidia
egl-wayland
libva-nvidia-driver

# ── GPU drivers — Intel iGPU + Vulkan loaders ────────────────────
intel-media-driver
libva-intel-driver
libva-utils
vulkan-intel
lib32-vulkan-intel
vulkan-icd-loader
lib32-vulkan-icd-loader
vulkan-tools
intel-gpu-tools
mesa
lib32-mesa
mesa-utils

# ── Audio firmware (CRITICAL — MSI GS77 needs sof-firmware) ──────
sof-firmware
alsa-firmware
alsa-ucm-conf

# ── Power / thermal management ──────────────────────────────────
thermald
acpid
upower
lm_sensors
nvtop
powertop

# ── Bluetooth + printing + webcam ───────────────────────────────
bluez-obex
cups
cups-pdf
system-config-printer
v4l-utils

# ── Biometric + hardware-token authentication ───────────────────
# fprintd  → fingerprint daemon (libfprint backend; works with most
#            laptop fingerprint sensors out of the box).
# pam-u2f  → PAM module for FIDO2/U2F hardware tokens (YubiKey).
# libfido2 → FIDO2 support library used by pam-u2f + ykman.
# yubikey-manager → CLI to register/program YubiKeys.
# pamtester → diagnostic tool, useful when wiring this stack up.
# python-opencv + python-dlib are runtime deps for `howdy` face auth
# (built from AUR by customize_airootfs.sh, since howdy is AUR-only).
fprintd
libfprint
pam-u2f
libfido2
yubikey-manager
# pamtester removed 2026-05-11 — AUR-only, built from source by
# customize_airootfs.sh (used at runtime by nyxus-bd-router for U2F auth).
python-opencv
# python-dlib removed 2026-05-11 — AUR-only, pulled in transitively by
# howdy's own debian/install.sh during the customize_airootfs.sh build.
# v4l2loopback-dkms — virtual V4L2 loopback device kernel module.
# Provides the v4l2loopback kernel module across the shipped kernel set.
v4l2loopback-dkms
# oath-toolkit ships oathtool (TOTP code generator/verifier) and pam_oath.
# Used by the backdoor stack as the 4th factor (rotating 30-second OTP).
oath-toolkit
# qrencode renders the TOTP enrolment URI as an ASCII QR code so the user
# can scan it into Aegis/Authy/Google Authenticator from a terminal.
qrencode

# ── Gaming stack (multilib) ─────────────────────────────────────
gamemode
lib32-gamemode
mangohud
lib32-mangohud
gamescope
giflib
lib32-giflib
gnutls
lib32-gnutls
libpng
lib32-libpng

# ── Daily-driver apps ───────────────────────────────────────────
firefox
nautilus
hyprshot
# Pixel eyedropper — Super+I copies any on-screen colour (Aurora pack).
hyprpicker
wf-recorder
file-roller
7zip
unrar
libcanberra

# ── archiso runtime requirements ──────────────────────────────────
arch-install-scripts
archinstall
pacman-contrib
reflector
rsync
which

# NYXUS installer (AUR-only; built in customize_airootfs.sh).

# ═══════════════════════════════════════════════════════════════════
# COMPLETION WAVE 1+2+3 — backends for half-built UI, hardware /
# network / FS completeness, stability + security hardening.
# Added 2026-05-13 to back the Settings UI pages that previously had
# frontend-only implementations (App Sandbox, Backup Snapshots,
# Accessibility, Notifications, Firmware Updates) and to fill the gaps
# audited in the 121-item completion master list.
# ═══════════════════════════════════════════════════════════════════

# ── Wave 1: backends for half-built UI pages ──────────────────────
# Settings → App Sandbox backend.
flatpak
# Flatpak portal base.
xdg-desktop-portal
# Settings → Notifications busctl backend.
swaync
# Settings → Firmware Updates backend.
fwupd
# mpv codec coverage (h264/aac/vorbis).
gst-plugins-good
# h265 / mkv / aac+.
gst-plugins-bad
# mp3 / x264 (proprietary).
gst-plugins-ugly
# ffmpeg-backed gstreamer plugins.
gst-libav
# general media transcoding + thumbnails.
ffmpeg
# accurate system time (TLS sanity).
chrony

# ── Wave 2: hardware / network / FS completeness ──────────────────
# CJK + missing standard font fallbacks.
noto-fonts-cjk
ttf-liberation
ttf-dejavu
cantarell-fonts
ttf-iosevka-nerd
adobe-source-code-pro-fonts
# NOTE (2026-07-14): dropped vulkan-radeon + lib32-vulkan-radeon — this is a
# hardware-locked build for the MSI GS77 (Intel Iris Xe + NVIDIA only). AMD's
# RADV Vulkan driver does nothing here; Vulkan resolves to vulkan-intel / the
# NVIDIA driver. Re-add only if this profile is ever retargeted to AMD hardware.
# Pin input driver explicitly (Hyprland normally pulls it but archiso
# image-build doesn't always include it).
xf86-input-libinput
# Removable media automount + GVFS backends (USB sticks, Android,
# SMB, iOS via gvfs-afc).
udisks2
udiskie
gvfs
gvfs-mtp
gvfs-smb
gvfs-afc
gvfs-gphoto2
gvfs-nfs
# VPN + mobile broadband + NM full GUI editor.
wireguard-tools
openvpn
networkmanager-openvpn
modemmanager
mobile-broadband-provider-info
nm-connection-editor
# mDNS / .local hostnames / AirPrint / printer auto-discovery.
avahi
nss-mdns
# Print/scan completeness.
# HP printer drivers (covers ~40% of home printers).
hplip
# Scanner backend.
sane
# Scanner GUI.
simple-scan

# ── Wave 3: stability + security + accessibility ──────────────────
# Out-of-memory protection (the single biggest stability win).
earlyoom
# Compressed RAM-backed swap (~2x effective RAM on small machines).
zram-generator
# Multi-core IRQ distribution + auto-nice.
irqbalance
# Sandboxing primitives (bubblewrap is also flatpak's runtime dep).
bubblewrap
firejail
apparmor
# Per-USB-device authorization (pairs with nyxus_usb_watch.py).
usbguard
# Kernel auditing (security daemon backend).
audit
# Encryption / keyring / signed packages.
gnupg
seahorse
libsecret
openssl
# Stateful firewall + zone-based config.
firewalld
# Accessibility stack (Settings → Accessibility backend).
orca
speech-dispatcher
espeak-ng
brltty
at-spi2-core
# Snapshot backups (Settings → Backup → Snapshots tab backend).
# NOTE: timeshift, snap-pac, snapper-rollback, ananicy-cpp, yay,
# distrobox are AUR — handled by customize_airootfs.sh build step.
snapper
restic
# Encrypted off-site backup alternative.
borg
# Dev / containers / power-user.
# Rootless containers.
podman
# Rust toolchain (modern AUR builds).
rustup
# Modern editor.
neovim
# Secondary browser.
chromium
# appimagelauncher is AUR-only — built from source by customize_airootfs.sh.
# Wayland tools that back existing services + UI conventions.
# Blue-light filter (backs nyxus-nightlight.service).
wlsunset
# Screenshot annotation (chained from grim/slurp).
swappy
# Boot-time multi-OS detection (off by default in stock Arch).
os-prober
# Misc system polish.
# ~/.cache/nyxus/*.log rotation.
logrotate
# Already listed above; grouping.
# man page database.
man-db
# base man-pages corpus.
man-pages

# ── NYXUS Drop / KDE Connect (cross-device file + text share) ──────
# DropPage in nyxus_settings.py auto-detects this and offers an install
# fallback if missing. Shipping it stock so first-boot is zero-config.
kdeconnect

# ── NYXUS Audio EQ (EasyEffects + LV2 plug-ins) ────────────────────
# SoundPage exposes preset switcher; presets ship in /etc/skel/.config
# /easyeffects/output/. Autostarted as a gapplication-service so the
# pipeline is hot before the user opens anything.
easyeffects
lsp-plugins-lv2
calf
# rnnoise model used by EasyEffects "Noise Reduction" preset.
rnnoise

# ── NYXUS Virt Manager (Tier B · 2026-05-14) ───────────────────────
# Full QEMU/KVM stack + libvirt + virt-manager UI + UEFI fw + bridged networking
# helpers + swtpm so Win 11 guests Just Work. nyxus-virt-setup runs
# on first launch from VirtPage to enable libvirtd + add user to grp.
qemu-desktop
libvirt
virt-manager
virt-viewer
edk2-ovmf
dnsmasq
swtpm
iptables

# ── NYXUS Containers (Tier B) ──────────────────────────────────────
# podman is already shipped above. Add buildah/skopeo for image build
# + transfer, distrobox for one-command rootless dev containers.
buildah
skopeo
distrobox

# ── NYXUS Kernel policy (rev 2026-07-23) ───────────────────────────
# NYXUS ships the custom "Kage Ryu" kernel as the PRIMARY/default kernel
# (staged into the ISO by build-iso.sh — NYX_WITH_KAGE_RYU is on by
# default) with stock `linux` (above) kept purely as an emergency RESCUE
# entry. The old linux-lts / linux-zen / linux-hardened extras were
# dropped — this is a focused custom-kernel distro, not a kernel buffet.
# nyxus-kernel-switch can still toggle the GRUB default between Kage-Ryu
# and stock.

# ── NYXUS Gaming / Proton-GE (Tier B) ──────────────────────────────
# Steam (multilib) + nyxus-protonup downloads Proton-GE releases into
# ~/.steam/root/compatibilitytools.d/ — no AUR dependency.
steam

# ── NYXUS Editors bundle (Tier B) ──────────────────────────────────
# code = open-source VS Code build. helix = modern modal editor.
# micro = nano replacement. gnome-text-editor = simple GUI default.
code
helix
micro
gnome-text-editor

# ── NYXUS Tier C (rev 2026-05-14): security & network polish ───────
# usbguard already shipped above (line 377). Adding strongSwan plug-in,
# Secure Boot manager (sbctl), TPM2 userspace tools, MAC randomizer,
# and dnscrypt-proxy for DNS-over-HTTPS.
#
# IMPORTANT: usbguard daemon ships with PresentDevicePolicy=allow and
# an EMPTY rules.conf so it never locks the user out of their own USB
# keyboard at install time. Per user pref: account-lockout / hardening
# policies are opt-in post-install only. UsbPage exposes the toggle.
networkmanager-strongswan
sbctl
tpm2-tools
macchanger
dnscrypt-proxy
sound-theme-freedesktop
qt5-svg

# ── NYXUS Ghost / Invisibility mode (rev 2026-07-17) ───────────────
# tor + torsocks back nyxus-ghost (invisibility/anonymity mode): the
# script toggles a Tor transparent proxy + nftables egress kill-switch,
# randomises the MAC (macchanger, above), and mutes mDNS (avahi). tor is
# TOGGLED by nyxus-ghost — its service is deliberately NOT enabled at boot
# (see customize_airootfs.sh: tor.service is absent from the enable list).
# nftables (Bifrost dep, above) provides the kill-switch ruleset.
tor
torsocks

# ── NYXUS greeter stack (rev 2026-07-14) — REQUIRED ────────────────
# The ISO enables greetd (see customize_airootfs.sh). The greeter chain
# nyxus-greeter runs regreet under cage, falling back to tuigreet (text).
# Without these three the ISO boots to greetd with NO greeter binary =
# broken login. cage/regreet are Wayland (dodges the SDDM X11 hybrid-GPU
# crash). SDDM is intentionally NOT installed anymore.
greetd-regreet
greetd-tuigreet
cage

# ── NYXUS security-lab toolkit (rev 2026-07-14) ────────────────────
# This build is a security lab (see docs/MACHINE_PROFILE.md): jeTT eBPF
# EDR, Docker honeypots, malware analysis. All official-repo (no AUR).
# eBPF / kernel observability (pairs with the kage-ryu sensor + BCC):
bpftrace
bpf
bpf-linker
# Traffic capture / recon:
nmap
tcpdump
wireshark-cli
# Reverse engineering / malware analysis:
radare2
rizin
ghidra
binwalk
yara
# Forensics / memory:
sleuthkit
volatility3
# Debug / trace:
gdb
strace
ltrace
trace-cmd

# ── NYXUS performance (rev 2026-07-14) ─────────────────────────────
# sched_ext userspace schedulers. scx_lavd (latency-aware) is enabled via
# /etc/default/scx + scx.service; runtime-swappable, reverts to EEVDF on stop.
scx-scheds

# ── NYXUS Master Hub (Bifrost) runtime deps (rev 2026-07-15) ───────
# Bifrost is the Tauri-based security dashboard staged into airootfs by
# build-iso.sh (binary already built off-tree at ~/Projects/bifrost).
# webkit2gtk-4.1/gtk3/libayatana-appindicator are required for the Tauri
# window + tray icon to render at all; the python-* set covers the
# guardian.py sidecar's imports.
#
# ★ CORRECTED 2026-07-27: ollama IS now listed. The previous note said it was
# "intentionally NOT listed - guardian.py already degrades gracefully without
# it (logs a non-fatal warning)". That assumption was WRONG and was measured
# on the live box: with ollama absent, heimdall.guardian gets
# [Errno 111] Connection refused on EVERY event, which means
#   CircuitBreaker guardian_extractor: OPEN   (permanently)
#   [!] AI queue full - dropped suspicious event   ~15,000 per hour
#   every verdict downgraded to action=LOG effective=LOG severity=INFO
# That is not graceful degradation - it is silent blindness, and the unit
# still reports `active` the whole time. Shipping ollama means a fresh
# install has a working AI EDR instead of one that only looks alive.
#
# NOTE the MODEL is not baked (941M): first boot still needs
#   ollama pull qwen2.5:1.5b-instruct     (or run: nyxus-edr-repair)
ollama
# suricata: host wire visibility. nyxus-suricata-setup ships in
# /usr/local/bin but the package did not, so on a fresh install the script
# had to pull it at runtime. The honeypots only see traffic aimed at the
# bait; nothing watched what actually crosses the NIC. Shipping it means
# `nyxus-suricata-setup` works offline on first boot.
suricata
webkit2gtk-4.1
gtk3
libayatana-appindicator
python-pydantic
python-openai
python-aiohttp
sqlite
nftables

# ── Meli (Honeypot Command Center) runtime deps (rev 2026-07-16) ──────
# Meli app + venv staged into airootfs by build-iso.sh from the installed
# /opt/meli (see PKGBUILD/install.sh). gtk4/libadwaita/python-gobject/
# python-cryptography/python-requests/python-bcrypt are already listed
# above for other NYXUS components; only the deps unique to Meli's
# PKGBUILD `depends=()` are added here. The shipped venv uses
# --system-site-packages, so these must be real pacman packages, not
# just pip installs baked into the venv.
mosquitto
python-sqlalchemy
python-click
python-structlog
python-paho-mqtt
python-yaml
python-argon2-cffi
python-pyotp
libnotify

# ── jeTT AI Security Daemon runtime deps (rev 2026-07-16) ─────────────
# jett-daemon itself is a self-contained Rust binary (ldd shows only
# libc/libgcc/libm — no extra runtime packages needed there); GPU
# inference depends on the nvidia-open-dkms/nvidia-utils/opencl-nvidia
# already listed above for the rest of NYXUS.

# ── honeypot/Docker stack runtime deps (rev 2026-07-16, r2) ────────────
# Needed to run the pre-pulled cowrie/conpot/dionaea/endlessh/heralding/
# http-honeypot + grafana/prometheus/loki/promtail containers staged into
# /opt/honeypot by build-iso.sh. `docker-compose` on Arch ships both the
# standalone binary AND the `docker compose` CLI plugin, so either
# invocation style works. iptables (already listed above) covers the
# DOCKER-USER egress-lockdown firewall.
docker
docker-compose

# ═══════════════════════════════════════════════════════════════════
# BLACKARCH — curated offensive/defensive toolkit (rev 2026-07-17)
# ═══════════════════════════════════════════════════════════════════
# The [blackarch] repo is wired in nyx-profile/pacman.conf; the host
# keyring (blackarch-keyring, populated on this build box) verifies these
# at pacstrap, and customize_airootfs.sh populates the keyring + adds the
# repo to the LIVE /etc/pacman.conf so post-boot installs keep working.
#
# SIZE TRADEOFF (documented per the task): the full `blackarch` group is
# ~2800 packages and would balloon the ISO to tens of GB. Instead this is
# a CURATED ~67-tool set spanning recon / scanner / webapp / exploitation /
# wireless / cracker / forensic / anti-forensic / sniffer / proxy / crypto —
# enough to be "loaded with the works" while the ISO still bakes + boots
# (adds roughly a few GB, dominated by metasploit/zaproxy/kismet). Anyone
# who wants EVERYTHING runs `nyxus-blackarch-full` post-boot (installs the
# whole `blackarch` group). Tools already shipped from official repos
# (nmap, wireshark-cli, radare2, rizin, ghidra, binwalk, yara, sleuthkit,
# volatility3, tcpdump, gdb, tor, torsocks, macchanger, …) are NOT relisted
# here to avoid duplicates.
# NOTE: blackarch package names are curated by hand; if any single name
# fails to resolve at the first real bake, remove that one line (pacstrap
# aborts on an unknown package) — everything else is independent.

# ── keyring + mirrorlist (so the LIVE system trusts + resolves blackarch)
blackarch-keyring
blackarch-mirrorlist

# ── recon / OSINT ──
amass
subfinder
assetfinder
theharvester
recon-ng
dnsrecon
dnsenum
fierce
sublist3r
# ── scanners ──
masscan
nikto
wpscan
nuclei
naabu
rustscan
enum4linux
# ── webapp ──
sqlmap
gobuster
ffuf
feroxbuster
dirb
wfuzz
whatweb
commix
xsser
dalfox
zaproxy
# ── exploitation ──
metasploit
exploitdb
routersploit
crackmapexec
impacket
# ── wireless ──
aircrack-ng
wifite
reaver
bully
bettercap
kismet
hcxtools
hcxdumptool
mdk4
pixiewps
# ── password / cracker ──
hashcat
john
hydra
medusa
ncrack
crunch
cewl
hashid
patator
# ── forensic / anti-forensic ──
foremost
bulk-extractor
# UNRESOLVED: steghide
stegseek
perl-image-exiftool
bleachbit
secure-delete
mat2
# ── sniffer / MITM / proxy ──
ettercap
dsniff
responder
mitmproxy
proxychains-ng
# ── SSL / crypto ──
sslscan
sslyze
testssl.sh

# ── INSTALLER ────────────────────────────────────────────────────────
# ★ THE FIX (2026-07-28): calamares is installed as a BINARY PACKAGE here,
# not built from the AUR in the chroot.
#
# Four ISOs shipped with no installer because customize_airootfs.sh tried to
# `makepkg` calamares inside the chroot, and that path kept breaking for a
# different reason each time - first an unanswerable `sudo` password prompt
# for the locked `nobody` user, then an empty chroot mirrorlist ("no servers
# configured for repository: extra"), so kpmcore + yaml-cpp were unreachable.
#
# None of that is necessary: calamares is a plain binary in [blackarch],
# which this profile's BUILD-TIME pacman.conf already enables, and both of its
# problem dependencies are in [extra]:
#     calamares -> blackarch      kpmcore -> extra      yaml-cpp -> extra
# pacstrap installs it directly, so the installer no longer depends on the
# chroot having working network, a build user, or a mirrorlist at all.
#
# The AUR `_aur_build calamares` call is left in place as a fallback - it
# early-returns when `pacman -Qi calamares` already succeeds, so it becomes a
# no-op rather than dead weight.
calamares
