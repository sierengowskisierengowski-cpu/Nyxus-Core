# NYXUS — FLAWLESS-BASELINE AUDIT (opened 2026-07-31)

> The owner's directive: stop adding features, drive everything done so far to
> a **known-good baseline** — nothing blank, dead, stale, or broken — so from
> here back is A1 and forward progress isn't building on unknowns. One ledger
> so nothing turns into hide-and-seek. Work items in verifiable chunks; mark a
> line DONE only after it's been proven (clicked / booted / re-verified).

## 0. Baseline facts (measured 2026-07-31)
- `iso-builder/verify-profile.sh` → **0 FAIL**, 18 WARN.
- `scripts/iso-build-verify.sh` → **0 FAIL**.
- Latest ISO `nyxus-2026.07.31-x86_64.iso` boots: splash→login **21s**, greetd
  login correct, background correct, Hub works. Installer = calamares (full
  wizard: locale→tz→keyboard→partition→**create user/pass/hostname**→install).

## 1. Build gates — WARN items (from verify-profile.sh)
- [ ] **Hyprland version skew** — ISO ships 0.56.0-2, dev box runs 0.55.4-1.
      Configs verified here aren't verified against what boots. Decide: bump
      the box, or pin the ISO to a known version. HIGH — it undermines every
      other "verified live" claim.
- [ ] **Dead station app branches** — stations.json: FORGE→`cursor`,
      WAVE→`strawberry`,`audacious`, CORE→`thunar` reference apps not on the
      ISO. Guarded (branch never fires) but stale. Repoint or drop.
- [ ] **10 eww namespaces missing an explicit `layerrule`** — deepcore, dock,
      dock-reveal, hub, mission, notif, snap-picker, snap-toast, splash,
      start-search, updates. Only get the catch-all floor → no explicit blur.
      **Prime suspect for the "shadow boxes."**
- [ ] customize_airootfs.sh not +x — cosmetic (mkarchiso chmods it). Low.

## 2. Live-boot findings (owner, first USB boot 2026-07-31)
Most were **live-USB first-boot-on-slow-USB** (one-time setup installing off the
stick), NOT permanent bugs — must be re-judged on an INSTALLED system:
- [ ] **postgresql.service prompts for polkit auth on login** — a service
      shouldn't ask the user to authenticate. Real. Needs a polkit rule or to
      not auto-start needing auth.
- [ ] **Shadow boxes** back (theming). Likely §1 layerrules.
- [ ] Stations/apps/Firefox "won't load", Settings won't open, notifications
      lag — **verify on installed system**; expected to clear once first-boot
      install completes. Don't chase on the live USB.
- [x] **`nyxbuild` build user reached the login** — FIXED (bulletproof teardown,
      commit 4077bbb2).
- [x] Boot speed / login correctness — GOOD.

## 3. Lock / greeter
- [ ] Frosted hyprlock + live spectrum — built (PR #84), darkened after the
      first pale-wash preview; **owner-preview pending**, not promoted live.
- [ ] hyprlock has stranded the session 3× during testing — the standing rule
      is now: **only the owner runs hyprlock**, never an agent. Verify
      `allow_session_lock_restore` shipped in the ISO's hyprland.conf.

## 4. Settings mandate (separate ledger: [[nyxus-settings-mandate]])
- [x] Themed to match system (glass+prism).
- [x] No-terminal 113→~5 (edge/admin escapes remain).
- [x] greetd login contract (greeting+clock reach regreet).
- [ ] Greeter weather/username/badge NOT wired (regreet limitation).
- [ ] Dead-control audit across ~57 sections — only Power/Screensaver done.
- [ ] Coverage sweep of ~174 nyxus-* tools; the 3 CLI-only tools; keybind viewer.
- [ ] The ~5 remaining terminal escapes (AUR, admin-passwd, visudo, evtest).

## 5. How to work this
1. **Install to disk** (calamares) and boot the installed system — the only
   honest environment to judge §2 runtime items. The live USB is not.
2. Fix §1 (build gates) — low risk, high signal, closes real dead/stale items.
3. Re-run both verifiers after each chunk; keep them at 0 FAIL.
4. Then §4 (settings mandate) in verified section batches.
5. Promote the frosted lock (§3) only after the owner previews it.

*Update this file as items close. A line is DONE only when proven, not when coded.*
