Library
Apps
NYXUS SysMon
NYXUS SysMonWebsite
NYXUS Widgets
NYXUS WidgetsWebsite
NYXUS Stickies
NYXUS StickiesWebsite
NYXUS Notepad
NYXUS NotepadWebsite
NYXUS Download Portal
NYXUS Download PortalWebsite
Images
ACHII — nyxus-wallpaper.png
ACHII — nyxus-wallpaper.pngImage
V2 — ACHII City Takeover
V2 — ACHII City TakeoverImage
V3 — ACHII Skull Death
V3 — ACHII Skull DeathImage
V4 — ACHII Global Hack
V4 — ACHII Global HackImage
NYXUS Hacker Wallpaper
NYXUS Hacker WallpaperImage
Graffiti Wall 1 — Wildstyle Tags
Graffiti Wall 1 — Wildstyle TagsImage
Graffiti Wall 2 — Subway Car
Graffiti Wall 2 — Subway CarImage
Graffiti Wall 3 — Alleyway Murals
Graffiti Wall 3 — Alleyway MuralsImage
Graffiti Wall 4 — Hall of Fame
Graffiti Wall 4 — Hall of FameImage
DEOF
ok "nyxus-notepad.desktop"

# Refresh app launcher cache
update-desktop-database "$DESKTOP_DIR" 2>/dev/null || true

# ── APPLY LIVE ───────────────────────────────────────────────────────────────
hdr "Applying Changes"

if command -v hyprctl &>/dev/null && [[ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ]]; then
  hyprctl reload &>/dev/null && ok "Hyprland config reloaded" || true
else
  printf "  ${DIM}Run ${PURPLE}hyprctl reload${DIM} to apply the new config${R}\n"
fi

if [[ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ]]; then
  pkill waybar 2>/dev/null || true
  sleep 0.8
  hyprctl dispatch exec "waybar --config $HOME/.config/waybar/config --style $HOME/.config/waybar/style.css"
  ok "Waybar restarted — 4-bar NYXUS layout active"
fi

if [[ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ]]; then
  pkill swaybg 2>/dev/null || true
  swaybg -i "$WALLS_DIR/nyxus-sierengowski-clean.png" -m fill &
  disown
  ok "Wallpaper set — nyxus-sierengowski-clean"
fi

if command -v makoctl &>/dev/null && [[ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ]]; then
  makoctl reload 2>/dev/null && ok "Mako reloaded" || true
fi

# ── SUMMARY ───────────────────────────────────────────────────────────────────
echo ""
printf "${DIM}──────────────────────────────────────────────────────────────────────${R}\n"
echo ""

if [[ $failed -eq 0 ]]; then
  printf "  ${GREEN}${B}NYXUS fully installed.${R}\n\n"
  printf "  ${GOLD}Wallpaper:${R} SIERENGOWSKI (permanent — set at boot)\n"
  printf "    ${DIM}Super+Alt+W  → reload wallpaper if it ever clears${R}\n\n"
  printf "  ${PURPLE}${B}Lock your screen:${R}  ${DIM}Super+L${R}\n"
  printf "  ${PURPLE}${B}Open launcher:${R}    ${DIM}Super+D${R}\n"
  printf "  ${PURPLE}${B}Screenshot:${R}       ${DIM}Super+Print  (region)${R}\n"
  printf "  ${PURPLE}${B}Logout menu:${R}      ${DIM}Super+Shift+E${R}\n\n"
  printf "  ${DIM}S I L E N T · D A R K · P U R E L Y   F U N C T I O N A L${R}\n"
else
  printf "  ${RED}${B}${failed} item(s) failed.${R}\n"
  printf "  ${DIM}Check your connection and re-run.${R}\n"
  exit 1
fi

echo ""

Production
Status
joseph published 2 minutes ago
Visibility
Public
Geography
North America
Type
Autoscale(2 vCPU / 4 GiB RAM / 3 Max)
See all usage
NYXUS Notepad
nyxus-core.replit.app/nyxus-notepad/
NYXUS Stickies
nyxus-core.replit.app/nyxus-stickies/
NYXUS SysMon
nyxus-core.replit.app/nyxus-sysmon/
NYXUS Widgets
nyxus-core.replit.app/nyxus-widgets/
773c8912
JS
jsierengowski
josephpublished about 2 hours ago
b84917de
JS
jsierengowski
josephpublished about 3 hours ago
cec0efd8
JS
jsierengowski
josephpublished about 3 hours ago
79ecc570
JS
jsierengowski
josephpublished about 3 hours ago
d8d796b6
JS
jsierengowski
josephpublished about 3 hours ago
b81f770b
JS
jsierengowski
josephpublished about 3 hours ago
25b53830
JS
jsierengowski
josephpublished about 4 hours ago
d2584f91
JS
jsierengowski
josephpublished about 4 hours ago
fcd48175
JS
jsierengowski
josephpublished about 4 hours ago
26db4da4
JS
jsierengowski
josephpublished about 4 hours ago
5fda9859
JS
jsierengowski
josephpublished about 4 hours ago
6d47280d
JS
jsierengowski
josephpublished about 5 hours ago
2ca5245c
JS
jsierengowski
josephpublished about 5 hours ago
b8ef2051
JS
jsierengowski
josephpublished about 5 hours ago
2e1a54b2
JS
jsierengowski
josephpublished about 6 hours ago
8d7274d2
JS
jsierengowski
josephpublished about 6 hours ago
7f7715ab
JS
jsierengowski
josephpublished about 7 hours ago
dae25ee9
JS
jsierengowski
josephpublished about 7 hours ago
742d43e2
JS
jsierengowski
josephpublished about 7 hours ago
f0a65680
JS
jsierengowski
josephpublished about 7 hours ago
Published your App

commands
2 hours ago

hey it doesnt look like the one i lives with the bloue that stat sides ways or whatever
2 hours ago

You want the tilted note aesthetic from the original web version. Let me check what it looked like:

Got it — you want the tilted neon notes with the colored glow, dot-grid background, and the 10% colored tint. The GTK4 version can do all of that using cairo — I'll rewrite stickies to draw the entire board with cairo exactly like the web version:

yes i want all of them to look like that style those were bad ass man
2 hours ago

All four — same energy, same neon cyberpunk style. Let me check the original web versions of each so I match them exactly:

The pages are not in a /pages subdirectory, let me check other paths.

Now let me read all 4 original web files to understand the aesthetic:

    Weather.tsx
    Notepad.tsx
    CpuCard.tsx or SysMon.tsx (for the sysmon aesthetic)
    We already have Stickies.tsx

Reading all the originals at once:

Now I have a clear picture of all 4 original web designs. Let me extract the key aesthetic elements:

Stickies (already noted):

    Rotated sticky note cards with transform: rotate(${note.rotation}deg)
    10% opacity colored background per note
    Neon border glow
    Dot-grid background
    Color swatches at bottom of each note

Weather:

    Full-screen animated background layer (rain, snow, stars, clouds)

    Large 7xl temperature display

    Condition badge with green neon border: border-[#39ff14] bg-[#39ff14]/10 text-[#39ff14]

    Wind speed and humidity cards with neon borders, a 5-day forecast section with purple styling, and a system initialization state with a spinning border animation

    Location entry screen and status indicator showing the day and system status

Notepad:

    Left sidebar with note list and a grid-patterned editor background using layered gradients to create the checkered effect

Now I'm adding the markdown toolbar with formatting buttons, a preview/edit toggle, and a status bar tracking word count, character count, and save state. The sidebar header displays the system name, and active notes get a neon left border highlight.

SysMon:

    System monitor header with version number, an SVG circular CPU visualization with pink neon glow effects, dark panels with neon borders, and a live indicator with a pulsing green dot

Now I'm converting these designs into four GTK4 Python scripts—a sticky notes app with a Cairo board featuring rotated notes and a dot grid background, a notepad with the grid pattern and markdown editing, and continuing with the remaining applications...

For

yes with that tilt i like stuff like that how you had the web version so if we can do more like that style id be really really happy
2 hours ago

Let me read all 4 existing GTK scripts first so I know exactly what I'm working with:

make sure every funtion works too some of it didnt in noites ot sticky when i went ot type it didnt want to also change the font to match the new style too please and the font glow some what and make sure notes/clipbord save any copy and paste in a history
2 hours ago

OK, complete overhaul of all 4 apps — I've read every file. Here's exactly what I'm fixing:

    Stickies: Full cairo board with actual cr.rotate() tilt, click → editor dialog (fixes typing)
    Notepad: Fix focus/typing, add clipboard history panel that captures every paste
    Weather: Cairo stat cards with neon tilt, glow font headers
    SysMon: Dot-grid panels, neon glow throughout

Writing all 4 now:

Now the notepad with working typing and clipboard history:

Now the weather with cairo neon stat cards and the sysmon with dot-grid panels:

Now sysmon with dot-grid panels and neon glow throughout:

Now rebuild and deploy to the API server:

Let me verify the install script and download registry includes all 4 files:

All 4 files registered. Let me do a quick verification that the API serves the scripts correctly:

Wait — the stickies is showing the old header. The build must cache the old files. Let me check how the build copies scripts:

The dist has the new files. The API just needed a moment after restart — let me verify it's serving the new version now:

It's still serving the old version. Let me check exactly where the route reads from:

The dist has the new files but something is off. Let me check the build.mjs to understand what gets copied:

The live API at nyxus-core.replit.app is the deployed production version — not the dev server. I need to redeploy to push the new scripts. Let me do that:

All 4 scripts are done and the dev server has the new versions. The live production URL (nyxus-core.replit.app) needs a redeploy to serve the new scripts. Let me suggest that now:

Here's a full breakdown of everything that was done across all 4 apps:

Stickies — completely rebuilt:

    Full cairo DrawingArea board — every note is cr.rotate()'d to its own tilt angle (-4° to +4°), exactly like the web version
    Dot-grid background with purple dots at 24px spacing
    3 glow layers per note (24px → 15px → 8px at increasing alpha) — notes pulse when pinned
    10% color tint overlay + dark bg + neon border per note's color
    Color swatches drawn right on the card — click a swatch to recolor
    Clicking a note opens a proper editor dialog — this is what fixed typing. The dialog has a real Gtk.Entry for title and Gtk.TextView for body, both get grab_focus() automatically
    Rotation is stable per note ID (deterministic seed), so notes don't shuffle on reload

Notepad — fully rewritten:

    Ctrl+V intercept via EventControllerKey → reads clipboard async before the paste happens, saves to ~/.nyxus/clipboard.json (max 50 entries)
    Right panel shows full clipboard history — click any entry to paste it at cursor
    Typing now works reliably: grab_focus() called on the body TextView via GLib.idle_add when switching notes, and a flag prevents recursive save triggers
    COPY button also adds to clipboard history
    Font glow on all cairo-drawn text using 8-pass offset rendering

Weather — enhanced:

    Stats are now cairo DrawingArea panels — wind at -2.5° tilt, humidity at +2.5° tilt with full neon glow card treatment
    Location + condition rendered with glow_text() directly on the scene
    5-day forecast fully redrawn in cairo with glow text

SysMon — enhanced:

    Every panel (neon_panel_bg) gets a dot-grid fill + neon border with 3 glow passes
    Header title, section labels all use glow_text() with 8-pass blur
    Process list color-codes by CPU (green → yellow → orange) directly in cairo
    CPU ring chart gets a glow stroke on the arc

After you click Publish, the live URL will serve all the updated scripts. Then on your machine:

curl -fsSL https://nyxus-core.replit.app/api/download/nyxus/nyxus_install.sh | bash

Republish
joseph published 2 minutes ago
Visibility:
Public

make sure to send me everything to download and update the new and fixes to run and command
2 hours ago

i usualy do thank you
2 hours ago

──────────────────────────────────────────────────────────────────────

NYXUS fully installed.

Wallpaper: SIERENGOWSKI (permanent — set at boot)
Super+Alt+W → reload wallpaper if it ever clears

Lock your screen: Super+L
Open launcher: Super+D
Screenshot: Super+Print (region)
Logout menu: Super+Shift+E

S I L E N T · D A R K · P U R E L Y F U N C T I O N A L

2026-04-27 17:47:24 - [swaybg-1.2.2/main.c:332] Found config * for output eDP-1 (AU Optronics 0x978F )

...xXx... ~ 13s
❯
2 hours ago

thats dop-e thank you looks bad ass
27 minutes ago

i can we change the inside of the weather app do more colors on the inside of it more colors and that of what i sent in the image im trying to show youi guess thelook i was kind of going for alont and the font also do the other apps as well as that too please
21 minutes ago

I can see from your images you want a much more vibrant, multi-color approach — each element getting its own bright neon color rather than everything in one tone. Let me update all 4 apps with full rainbow palette usage throughout.

Now update the other 3 apps with the same full-palette rainbow treatment:

also change the looks of the design so its looks more of what i sent to you in those imagess also the app with graphs we need to do so much better then that and cover more sections of everything we can plull live data from also akk needs to be top of the line top tier enterprise grad start to finish withall of them being full loaded with what it can be for funtions
18 minutes ago

Big upgrade across all 4. Let me plan what "enterprise grade, fully loaded" means for each, then write them all:

    SysMon: 8-tab dashboard — Overview, CPU, Memory, Network, Disk, Processes (with search/sort/kill), Sensors (temps/battery/fans), System Info. Pull all live data psutil has + GPU via nvidia-smi
    Weather: More data fields + rainbow multi-color panels
    Notepad: Tags, sort, export, reading time, markdown preview
    Stickies: Drag to reposition, double-click edit, search overlay

Now update the notepad with enterprise features and stickies with drag support:

Now update stickies with drag-to-move, then rebuild and deploy:

All 4 apps written. Now rebuild and deploy:

All 4 apps rebuilt and API server is live. You now need to publish the project so nyxus-core.replit.app serves the new files, then reinstall.

Here's what's new in each app:

SysMon — 8-tab enterprise dashboard:

    OVERVIEW — 4 ring stat cards + 3 live history charts
    CPU — big ring, per-core vertical bars, CPU info panel, history chart
    MEMORY — RAM + SWAP detailed with history + top memory consumers list
    NETWORK — upload/download speeds, per-NIC breakdown, connection states, throughput history
    DISK — all partitions with usage bars + disk I/O read/write rates per device
    PROCESSES — searchable/sortable table, kill button with confirmation dialog
    SENSORS — all temperature chips, battery %, GPU via nvidia-smi, fan RPMs
    SYSTEM — OS, kernel, hostname, uptime, full hardware info

Notepad — Enterprise features added:

    Tags (comma-separated, color-coded per tag)
    Sort by date / title / word count
    Pin important notes
    Export to Markdown file (~/.nyxus/exports/)
    Reading time estimate in status bar
    Note color picker with 6 neon colors
    Word/char/sentence count in live status bar

Stickies — Now fully draggable:

    Drag any note anywhere on the board
    Double-click to open editor
    Rainbow multi-color dot grid (each dot cycles through the palette)
    Notes stay where you drop them (saved)

Weather — More stat tiles with full 6-color rainbow

Once you publish and reinstall:

curl -fsSL https://nyxus-core.replit.app/api/download/nyxus/nyxus_install.sh | bash

both of these are different should they be? oneopen up to that ifi use the x button on botton bar the other opens up the other one if i use Super+D but besides that we need to change the theme of those im thinking all pure white with glow then then paint splatters through out the theme too
6 minutes ago

Those two are different - one is bound to the taskbar button (grid style with icons) and the other is Super+D (list style). They can stay different in layout but should share the same visual theme. Let me read the current configs first:

Got it — they're separate by design (grid for taskbar, list for Super+D) but they'll now share the same white+glow+paint splatter aesthetic. Writing both now:
Published your App
7:14 pm, Apr 27, 2026

Now rebuild to bundle the new theme files:

All three rofi theme files are bundled. Publish the project now and reinstall:

curl -fsSL https://nyxus-core.replit.app/api/download/nyxus/nyxus_install.sh | bash

Here's what changed and the answer to your question:

Are they supposed to be different?
Yes — they're two different tools on purpose:

    Super+D opens the list-style launcher (nyxus.rasi) — fast keyboard-driven search, one result per row
    Taskbar button opens the grid-style launcher (startmenu.rasi) — visual app grid, click to launch like a start menu

You can use both or just one, up to you.

New theme — Pure White + Neon Paint Splatter:

    Background: dead black canvas (#06030f)
    All text: pure white (#ffffff) — no more purple/lavender tones
    White glow border on the window (2px bright white)
    White frosted glass input bar (10% white overlay, 50% white bottom border)
    Selected items: white paint blob effect (rgba(255,255,255,0.13–0.16) bg) with a bright neon left splatter stripe (pink #ff00ff or purple #cc00ff depending on row)
    Alternating rows get subtle different white tints
    Mode buttons (Apps/Run/Windows): white outline, white text when active, pink splatter border on selected

okay sounds good also remember do white as the main baground color with the mutlti colored paint splatter to match our theme also can so do something like that for the logout Menu that still hasnt been done either
3 minutes ago

Flipping rofi to white backgrounds, and adding the wlogout theme. Let me check what wlogout files exist first:

Got the full picture. Now rewriting both rofi themes (white bg this time) and the wlogout theme all at once:

Now the wlogout — white canvas with paint splatters at each corner matching the NYXUS palette:

Now rebuild to bundle everything and then check the wlogout tar gets rebuilt too:

The install script pulls `wlogout-style.css
Queue

stick notes dosent look diddferent and iot dont have that tiled looks with the doifferent color edges