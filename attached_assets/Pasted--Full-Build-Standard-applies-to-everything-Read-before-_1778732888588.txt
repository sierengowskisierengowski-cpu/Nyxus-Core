"Full Build Standard applies to everything. Read before starting.
This is a professional enterprise-grade operating system build. Everything must be built and shipped complete. No stubs, no placeholders, no empty options, no toggles that do nothing. If it is not complete it does not get merged. Match everything to the existing NYXUS brand already in the codebase — do not change any existing colors, fonts, or brand elements.
You are full stack — UI and backend together every time. No handoffs.
You are currently on Sprint B — audit and polish every existing GTK4 app, Settings hub, EWW bar, dock, and wallpaper studio for full visual consistency. Nothing off-brand, nothing inconsistent, everything at the same level. Every app must feel like it belongs in the same OS built by the same hand.
Additionally fix these two things as part of Sprint B:
First — every single app, window, overlay, panel, flyout, and dialog must open at an intelligent default size appropriate for what it is. Not full screen, not too small. Terminal opens comfortable for reading, Notes at a writing friendly width, Calculator compact, File Manager wider, Settings at standard system settings size. Every app must be fully responsive — if the user resizes the window everything inside reflows and fills the space cleanly. No empty dead space, no content crammed into a corner, no labels cut off, no buttons disappearing. Everything scales and fills proportionally and looks intentional at any size.
Second — the desktop must feel like a real modern OS natural to anyone coming from Windows or macOS. Every window must be freely movable by clicking and dragging the title bar. Windows freely resizable by dragging any edge or corner. Double clicking title bar maximizes and restores. Right clicking title bar shows Move, Resize, Minimize, Maximize, Close. Right clicking the desktop background opens a context menu with Change Wallpaper, New Folder, New File, Open Terminal Here, Display Settings, Refresh Desktop. Left clicking desktop icons selects them, double click opens. Desktop icons freely draggable and repositionable. Every window has a visible title bar with minimize, maximize, and close buttons on the right, clean and consistent across every app. Every open window appears in the taskbar. Clicking taskbar entry focuses or minimizes. Right clicking taskbar entry shows Close, Minimize, Move to Workspace. Every app and every element must have a right click context menu with relevant actions. Tiling stays available as opt-in via keybinds for power users but floating is the default experience.
Full Build Standard on everything. Verify-profile green before PR. Full changelog and green checklist. No stubs, no placeholders, nothing incomplete. If it is not complete it does not get merged."**