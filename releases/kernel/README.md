# Kernel Artifacts

Put custom kernel release artifacts in this directory.

Typical contents:

- kernel package files (`*.pkg.tar.zst`)
- matching headers/modules packages
- checksum/signature files
- short changelog or compatibility notes
