# ============================================================
#  NYXUS — Per-window opacity (FEATURE 2 of frosted-white)
#  ~/.config/hypr/conf.d/nyxus-hyprland-opacity.conf
#
#  r2 · 2026.05.05 — Seattle Frost spec values (still notepad canary)
#
#  Format: windowrule = opacity ACTIVE INACTIVE, class:REGEX
#  Spec values: 0.85 active / 0.70 inactive (per your config dump)
#  Combined with chrome.py r11 cream-frosted backgrounds + blur
#  brightness=1.4, this produces the misty-glass look the wallpaper
#  wants.
#
#  CANARY: nyxus-notepad only.  Once verified, flip to global by
#  removing the class regex (see commented-out global rule below).
#
#  © 2026 JOSEPH SIERENGOWSKI · NYX-J5W-2026-SIERENGOWSKI-LOCKED
# ============================================================

# Notepad canary — frost spec values
windowrule = opacity 0.85 0.70, class:re:^(nyxus-notepad|notepad)$

# === GLOBAL ROLLOUT (uncomment after notepad approval) ============
# windowrule = opacity 0.85 0.70, class:re:^(nyxus-.*)$
